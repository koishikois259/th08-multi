name: reconstruction-ledger

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Validate tracked reconstruction state
        run: |
          python3 -m py_compile scripts/*.py scripts/typed_re/*.py
          bash -n scripts/create_th08_prefix scripts/wineth08
          python3 scripts/validate-tracking.py
          python3 scripts/check-match-config.py
          python3 scripts/progress.py --check
          git diff --check
